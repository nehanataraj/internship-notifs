# Vercel / local dev — never commit real values

# Resume tailor API (Fly.io) — set after deploy
RESUME_API_URL=https://resume-tailor.fly.dev

# Recruiter template — OpenAI (same key as resume tailor)
OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
